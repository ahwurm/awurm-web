version: 1
appRoot: .
frontend:
  framework: web
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build:static
  artifacts:
    baseDirectory: out
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
